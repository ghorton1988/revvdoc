// TODO Phase 3: Service history timeline
// - getHistoryByCustomer(uid) → timeline sorted newest first
// - Each entry: service type icon, date, vehicle, cost, tech notes
// - Filter by vehicle dropdown
// - Tap entry → booking detail

export default function HistoryPage() {
  return (
    <div className="p-4 space-y-4">
      <h1 className="text-2xl font-bold text-text-primary">Service History</h1>
      <p className="text-text-muted text-sm">Phase 3 — not yet implemented</p>
    </div>
  );
}
