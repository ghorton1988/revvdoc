// TODO Phase 3: Notifications list
// - getNotifications(uid) → list sorted newest first
// - markAsRead(notifId) on tap
// - markAllAsRead CTA
// - Tap booking-related notifications → deep-link to booking/job page

export default function NotificationsPage() {
  return (
    <div className="p-4 space-y-4">
      <h1 className="text-2xl font-bold text-text-primary">Notifications</h1>
      <p className="text-text-muted text-sm">Phase 3 — not yet implemented</p>
    </div>
  );
}
